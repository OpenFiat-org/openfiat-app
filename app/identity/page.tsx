export default function Page() {
  return (
    <section>
      <h1 className="text-2xl font-semibold text-white">Identity & reputation</h1>
      <p className="mt-1 text-sm text-gray-400">Your identity claims and reputation history.</p>
      
      <div className="mt-6 rounded-lg border border-dashed border-white/15 p-8 text-center text-sm text-gray-500">
        No data yet — this view will populate once connected to an OpenFiat node.
      </div>
    </section>
  );
}
